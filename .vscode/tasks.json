{
    "version": "2.0.0",
    "tasks": [
        {
            // The task label (preLaunchTask in launch.json)
            "label": "MakeAndDebug",

            // Run the make command in the shell
            "type": "shell",
            "command": "make",
            "problemMatcher": [
                "$gcc"
            ],
            "group": "build",
            "detail": "Runs your Makefile before launching GDB."
        },
        {
            "type": "cppbuild",
            "label": "C/C++: gcc build active file",
            "command": "/usr/bin/gcc",
            "args": [
                "-fdiagnostics-color=always",
                //"-I${workspaceFolder}/include",
                "-g",
                "${file}",              //works with C/C++ extension version 1.18.5
                //"${workspaceFolder}/src/*.c", <------option 1
                // "$(find", <-------------------------option 2
                // "${workspaceFolder}/src",
                // "-type",
                // "f",
                // "-iregex",
                // "'.*\\.c')",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger."
        }
    ]
}